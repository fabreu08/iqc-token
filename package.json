{
  "name": "immutableqc-token",
  "version": "1.0.0",
  "description": "IQC Token and on-chain allocation contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat ignition deploy ignition/modules/IQCTokenWithAllocations.ts --network baseSepolia",
    "deploy:base": "hardhat ignition deploy ignition/modules/IQCTokenWithAllocations.ts --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.5"
  }
}